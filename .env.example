# Copy to .env and fill in real values before deploying.
# Without these, the app falls back to insecure defaults (fine for local dev only).

SECRET_KEY=change-me-to-a-long-random-string
ADMIN_PASSWORD=change-me
DEBUG=false
